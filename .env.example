# Copy to .env and fill in. .env is gitignored and must stay untracked --
# it held a real key on a public repo for two months before anyone noticed,
# and gitignore does not protect a file that is already committed.
#
# In CI these come from GitHub Actions secrets, not from a file.
OPENAI_API_KEY=
