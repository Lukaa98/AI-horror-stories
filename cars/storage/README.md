# Media storage

This directory is the clean storage scaffold for reviewed media:

- `images/` stores approved source images.
- `videos/` stores approved rendered videos.

Generated workflow drafts remain under `cars/drafts/<draft-id>/` on the
`cars-output` branch. Do not commit generated draft media to `v10`.
