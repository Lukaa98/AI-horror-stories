name: Deploy Cars UI to GitHub Pages

on:
  push:
    branches:
      # The branch being developed on. This has to move with it -- it sat on
      # v10 after work had already moved, which would have left the dashboard
      # frozen at an old build while changes were pushed past it.
      - v11
      - develop
  workflow_dispatch: # allow manual runs

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: web
        run: npm install

      - name: Build project
        working-directory: web
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: web/dist
